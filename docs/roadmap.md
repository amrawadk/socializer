# Road Map

At a high level, here're the different components planned:

<!-- TODO Add high level architecture -->
``` mermaid
graph LR
  A[Start] --> B{Error?};
  B -->|Yes| C[Hmm...];
  C --> D[Debug];
  D --> B;
  B ---->|No| E[Yay!];
```